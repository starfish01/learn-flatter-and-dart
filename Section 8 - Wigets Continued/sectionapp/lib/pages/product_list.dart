import 'package:flutter/material.dart';

class ProductListePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Product list'),
    );
  }

}